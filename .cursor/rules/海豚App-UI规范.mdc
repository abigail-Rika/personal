---
description: 海豚App通用UI设计规范（HaitunApp DesignUIKit），包含颜色、字体、圆角、导航栏、按钮、图标、列表、选项卡、反馈组件、数据录入等完整规范
alwaysApply: false
---

# 海豚 App 通用 UI 规范

> 来源：`海豚项目/UI规范/海豚App/` 设计稿

---

## 一、基础：颜色

### 品牌色

| 名称 | 色值 | 用途 |
|------|------|------|
| 品牌主色 | `#6075FF` | 主要品牌色调 |
| 品牌辅助色 | `#6372FF` | 辅助品牌色调 |
| 售课主色 | `#FF641A` | 售课/商业化场景 |

### 中性色（文字与背景）

| 名称 | 色值 | 用途 |
|------|------|------|
| 主要文字色 | `#191919` | 标题、正文 |
| 次要文字色 | `#191919` 82% 透明度 | 次要说明文字 |
| 次级文字色 | `#191919` 62% 透明度 | 辅助信息 |
| 提示文字色 | `#191919` 42% 透明度 | placeholder、提示 |
| 置灰颜色 | `#191919` 20% 透明度 | 禁用状态 |
| 分割线颜色 | `#E3E3F3` | 分割线 |
| 页面白色背景 | `#F7F8FC` | 浅色页面背景 |
| 页面灰色背景 | `#F4F6FD` | 灰色页面背景 |

### 功能色

| 名称 | 色值 | 用途 |
|------|------|------|
| 警示色 | `#FA5161` | 错误、警告 |
| 错误色 | `#FF6D32` | 输入错误 |
| 成功色 | `#16D5E2` | 成功反馈 |
| 蓝色 | `#3CB7FF` | 链接、信息 |
| 紫色 | `#5488FF` | 强调 |
| 黄色 | `#FFC500` | 提醒、奖励 |

### 学科色

| 学科 | 色值 |
|------|------|
| 语文 | `#FF5F33` |
| 数学 | `#A0FFF` |
| 英语 | `#FF8000` |
| 物理 | `#3C665C` |

---

## 二、基础：字体

### 字体选择

- **iOS**：PingFang SC（苹方）
- **Android**：FZLanTingHei-R-GBK（兰亭黑）、Noto Sans
- **数字专用**：DIN 字体（用于积分、倒计时等数字展示）

### 字号阶梯

中文字号（PingFang / FZLanTingHei）：32 / 28 / 24 / 22 / 20 / 18 / 16 / 14 / 12 / 10 pt

数字字号（DIN）：40 / 32 / 28 / 24 pt

英文字号（Regular）：30 / 24 / 23 pt

### 行高规则

**行高 = 字号 + 8**

常用字号行高对照：

| 字号 | 行高 |
|------|------|
| 32pt | 40 |
| 28pt | 36 |
| 24pt | 32 |
| 22pt | 30 |
| 20pt | 28 |
| 18pt | 26 |
| 16pt | 24 |
| 14pt | 22 |
| 12pt | 20 |

---

## 三、基础：圆角

| 圆角值 | 适用场景 |
|--------|----------|
| 24 | 弹窗、卡片、条目 |
| 20 | 卡片区 |
| 16 | 弹窗 |
| 12 | 常用组件 |
| 8 | 输入区域、小组件 |

使用原则：主要圆角使用不超过 7 种，大圆角用于大容器，小圆角用于小组件。

---

## 四、通用组件：导航栏

### APP 导航

- **高度**：状态栏 + 导航栏（Nav 高度 ≈ 56px）
- **标题**：居中显示，最多 10 个汉字，字号 16-17pt
- **返回按钮**：左侧「<」图标

### 导航栏变体

1. 纯返回（无标题）
2. 返回 + 标题
3. 返回 + 标题 + 右侧文字按钮
4. 返回 + 标题 + 右侧图标按钮
5. 返回 + 搜索栏
6. 返回 + 标题 + 多个右侧操作
7. 大小字混合标题

### 平板/横屏导航

与手机导航结构一致，但宽度拉伸，标题和操作区域保持相对位置。

### 小程序导航

底部 Tab 切换样式，标题居中，左侧无返回按钮。

---

## 五、通用组件：按钮

### 按钮层级

| 层级 | 名称 | 用途 |
|------|------|------|
| 主要 Button | 实心填充 | 页面核心操作（每个页面最多 1 个） |
| 次要 Button | 描边/线框 | 次要操作 |
| 幽灵 Button | 透明底+描边 | 可选操作 |
| 文字 Button | 纯文字 | 辅助操作 |

### 按钮状态

三种状态：**默认** → **点击**（按下） → **禁用**（置灰不可点击）

### 按钮尺寸

| 尺寸 | 说明 |
|------|------|
| 大 | 全宽或大面积操作 |
| 中 | 常规操作 |
| 小 | 紧凑空间 |
| 超小 | 行内操作 |

### 按钮颜色

按钮颜色跟随品牌色（`#6075FF` 紫色系），特殊场景使用功能色（橙色、红色等）。

### 组合规则

- 多按钮横排时保持间距一致
- 主要按钮与次要按钮搭配使用
- 底部固定操作栏中按钮通常为全宽

---

## 六、通用组件：图标

### 设计栅格

基础栅格 **24 x 24px**，在栅格内设计保证视觉统一。

### 图标类型

1. 线框参考线图标
2. 圆形图标
3. 填充图标
4. 菱形图标
5. 大尺寸图标

### 推荐尺寸

16 / 24 / 32 / 36 / 40 / 48 / 56 / 120 px

### 设计原则

- 在栅格内居中对齐
- 保证不同形状图标的视觉重心平衡
- 笔画粗细和边距保持一致
- 放大与缩小时保持清晰度

---

## 七、通用组件：列表

### 组件类型

- **单行**：标题（+ 右侧箭头/开关/标签）
- **双行**：标题 + 副标题（+ 右侧箭头/开关/标签）

### 可组合元素

左侧：图标、头像、勾选框
右侧：箭头、开关、标签、文字说明

### 特殊列表

部分场景有特殊列表样式（如教研物料列表、数字索引列表等），遵循各自独立规范。

---

## 八、通用组件：选项卡（Tabs）

### 分布方式

- **均分选项卡**：各 Tab 等宽平分容器
- **等距选项卡**：各 Tab 间距相等，宽度由内容决定

### 主题样式

- **下划线**：选中 Tab 底部有下划线指示
- **标签**：选中 Tab 以填充标签样式呈现

### 尺寸

- 小尺寸 / 大尺寸

### 状态

- 选中态（高亮 + 下划线/填充）
- 默认态
- 禁用态（文字置灰）

### 约束

- 选项卡数量建议不超过 5 个（最多 5 个）
- 单个选项卡字数建议不超过 9 个汉字

---

## 九、反馈组件

### 加载（Loading）

- **类型**：纯图标 / 图标 + 文本
- **图标**：三种背景透明度（白色底、黑色底、透明底），其中黑色底仅用于视频环节，透明底仅用于学习计划加载中
- **文本限制**：每行最多 10 个汉字（20 字符），超出换行，建议不超过 3 行

### 轻提示（Toast）

- **类型**：纯文本 / 图标 + 文本
- **最小宽度**：120px
- **文本限制**：每行最多 12 个汉字（24 字符），建议不超过 3 行
- **行高**：22（字号 + 6）
- **位置**：屏幕垂直居中
- **行与行规则**：行高 22，字号 + 8

### 对话框（Dialog）

四种类型：
1. **提示弹窗**：标题 + 内容 + 按钮组
2. **输入弹窗**：含文本输入框
3. **输入弹窗（上传）**：含上传操作
4. **功能弹窗**：自定义内容区域

设计建议：
- 设计时每个弹窗有明确唯一目的
- 不要在弹窗中嵌套弹窗
- 关键信息放在内容区，不要放在标题中

**运营弹窗**：全屏营销弹窗（540 x 810px），支持图片 + 按钮组合，有独立视觉风格。

### 动作面板（Action Sheet）

- **列表类**：纯文本选项列表（+ 可选勾选框）
- **图标列表类**：图标 + 文本选项
- **使用场景**：分享、上传/修改头像等
- 底部固定「取消」按钮

### 弹出层（Popup）

- **顶部弹出**：从屏幕顶部下滑
- **底部弹出**：从屏幕底部上滑
- **使用场景**：反馈输入、选项选择、筛选条件

---

## 十、数据录入

### 输入框

| 类型 | 说明 |
|------|------|
| 密码输入 | 掩码显示，可切换明文 |
| 搜索输入 | 搜索图标 + 输入框 + 清除按钮 |
| 验证码 | 分格输入或单行输入 + 发送按钮 |
| 评论 / 短文本 | 单行输入 + 发送按钮 |
| 多行输入 | 自适应高度，含字数统计 |

### 选择器

| 类型 | 说明 |
|------|------|
| 单列选择器 | 单个滚轮列 |
| 双列选择器 | 两个联动滚轮列 |
| 三列选择器 | 三个联动滚轮列（如省市区） |

选择器结构：顶部操作栏（取消 + 标题 + 确定） + 滚轮区域。
