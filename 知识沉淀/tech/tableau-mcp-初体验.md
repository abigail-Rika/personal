# Tableau MCP 初体验

> 日期：2026-03-02

## 发现

- Tableau MCP 的能力边界取决于 Tableau 里有什么数据。不是所有数据分析需求都能走这条路，需要先确认数据源覆盖范围。
- 验证 Tableau 集成时，应该挑一个 Tableau 里**已有数据**的需求来做，而不是拿一个数据不在 Tableau 里的任务硬凑。

## 待验证

- [ ] 找一个 Tableau 现成有数据的日常需求，跑通"提问 → 数据 → 洞察"的完整流程
- [ ] 记录哪些环节 AI 能自主完成、哪些需要人引导
