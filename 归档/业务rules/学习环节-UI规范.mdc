---
description: 海豚学习环节UI设计规范，包含Phone/iPad布局框架、适配规则、环节专属组件（按钮/导航/题目选项/拖拽填空/翻页器/图标）、学科色体系、字重规范
alwaysApply: false
---

# 学习环节 UI 规范

> 来源：`海豚项目/UI规范/学习环节/` 设计稿
> 学习环节是海豚AI课的核心教学场景，有独立于App通用规范的专属组件和布局体系。

---

## 一、布局框架

### 1. Phone 端

#### 1.1 横屏（核心场景）

**基础框架 — 双栏布局**：
- **左侧**：内容区域（题目、文章、板书等），占比约 40%-70%
- **右侧**：板书/互动区域（AI板书、学生交互），占比约 30%-60%
- **顶部**：导航栏（返回 + 标题/进度 + 积分/操作）
- **底部**：操作栏（固定高度，放置主要操作按钮）

布局约束：
- 内容区最小宽度约 40%
- 板书区根据内容弹性伸缩，最小 196px，最大 768px
- 底部操作栏固定高度

**基础框架 — 面板叠加布局**：
- 面板可从侧方滑入或浮层覆盖在内容区之上
- 板书区域最低高度约 96px
- 适用于需要临时展示额外信息的场景
- 交互方式为内容面板叠加覆盖

**特殊框架**：
- 全屏模式，不分左右栏
- 适用于特殊教学环节（如视频播放、大面积图形展示）
- 内容区占满整个屏幕，底部操作栏可隐藏

#### 1.2 竖屏

**通栏框架**：
- 全宽纵向布局，导航在顶部
- 内容区域可纵向滚动
- 底部固定操作栏

**卡片框架**：
- 卡片式内容展示
- 适用于阅读类、展示类环节

### 2. iPad 端

#### 2.1 横屏

**基础框架**：
- 与 Phone 横屏类似但更宽敞
- 左右分区更清晰，间距更大

**两栏大框布局**：
- 两栏并列，各栏内可放置大面积内容（如文章 + 板书）
- 板书区域可包含选项卡（题目/文章/板书切换）

**三栏通知栏布局**：
- 三栏结构用于需要同时展示多类信息的场景

**特殊框架（三栏通知栏 + 通栏布局）**：
- 适用于信息密集型环节
- 可同时展示导航通知 + 主内容 + 侧栏信息

#### 2.2 竖屏

- 两栏大框的竖屏适配
- 三栏通知栏的竖屏适配
- 整体按竖向重排，内容区域纵向堆叠

---

## 二、适配规则

### 核心适配原则

| 目标设备 | 适配策略 |
|----------|----------|
| **Phone 横屏** | 设计基准（1x），所有组件以此为标准尺寸 |
| **iPad 横屏** | 基于 Phone 端 **1.5 倍**放大 |
| **竖屏（Phone/iPad）** | 根据屏幕宽度**等比放大** |

### 基础按钮适配

- **横屏适配**：保留微页面的通用页面，基于 Phone 端 1.5 倍放大；组件系统按照客户端效果，保持 Phone 尺寸自适应
- **竖屏适配**：根据屏幕宽度等比放大

### iPad 样式说明

- iPad 竖屏以 1024 宽度为基准
- iPad 横屏以 1.5 倍示意（相对 Phone）

---

## 三、环节组件

> 组件化原则：将**稳定且具备通用性的原子和分子**做成组件，目的是**统一和提效**。
> 已组件化内容：导航、按钮、选择题、拖拽题、标签、图标、翻页器。

### 3.1 Button（学习环节按钮）

区别于 App 通用按钮，学习环节按钮有独立的视觉风格。

#### 按钮类型

| 类型 | Phone 横屏 | iPad 横屏 | 说明 |
|------|-----------|-----------|------|
| 主要按钮 | 实心填充（紫/橙，随学科色） | 尺寸放大 | 核心操作 |
| 次要按钮（描边） | 描边线框 | 尺寸放大 | 次要操作 |
| 次要按钮（浅底） | 浅色填充底 | 尺寸放大 | 辅助操作 |
| 幽灵按钮 | 浅色描边、文字跟随学科色 | 尺寸放大 | 可选操作 |
| 组合按钮 | 文字 + 右侧箭头图标 | 尺寸放大 | 引导性操作（如"继续学习 →"） |

#### 按钮状态

三种状态：**默认** → **点击**（按下高亮） → **禁用**（整体置灰）

#### 横屏尺寸规范

- 基础按钮：button 内心要素最大宽度 68px，字号按需加小圆图标
- 特殊按钮可容纳超长文案，文字超 70% 后截断适配
- 组合按钮：按钮尺寸 32px，文字 + 箭头图标组合

### 3.2 导航（学习环节导航）

学习环节导航有四套布局变体：

#### 竖屏导航

**基础导航（新增积分）**：
- 返回按钮（<） + 右侧积分（金币图标 + 数字）
- 标题可选展示

**基础导航 + 标题**：
- 返回 + 居中标题 + 右侧积分

#### 横屏导航

**基础导航（新增积分+跳过）**：
- 返回 + 标题 + 积分 + 跳过按钮

组合元素说明：

| 元素 | 说明 |
|------|------|
| 返回 | 左侧「<」图标，固定 |
| 积分样式 | 金币图标 + 数字（如"2333"），右上角位置 |
| 标题 + 积分样式（标题+翻页器的总字数限制） | 标题字号 16pt，字号不超过可用宽度 7 个汉字 |
| 进度条 | 线性进度指示 |
| 翻页器 | XX/XX 格式（如 05/06） |
| 跳过 | 文字按钮"跳过"，右侧位置 |
| 副标文字 | 标题下方副标题 |

**个性化导航样式**：
- 支持导航栏下方展示任务提示文字（如"小明同学是否去过很多不同的城市游玩？请选择你觉得推荐的地方吧"）
- 提示文字区域有独立背景色和间距

#### iPad 导航适配

- iPad 竖屏（1024 宽度示意）
- iPad 横屏（1.5 倍示意）
- 横屏适配：保留微页面通用页面，基于 Phone 端 1.5 倍放大；做到来和端效果保持 Phone 尺寸自适应
- 竖屏适配：根据屏幕宽度等比放大

### 3.3 题目选项

#### 选择题

**Phone 横屏**：
- 默认态：灰底白字圆角卡片，左侧序号（A/B/C/D）+ 选项文案
- 选中态：底色加深
- 答对：绿色描边 + 绿色勾（✓）
- 答错：红色描边 + 红色叉（✗）

**iPad 横屏**：
- 结构相同，尺寸放大
- 选项卡片可容纳更长文案（多行自适应）

#### 判断题

**Phone 横屏**：
- 两个按钮：「正确」和「错误」
- 默认态：灰底 + 图标
- 选中态：背景高亮
- 结果态：正确→绿色填充+勾，错误→灰色+叉

**iPad 横屏**：
- 结构相同，按钮尺寸放大

#### 题目选项适配说明

- 选择题选项高度不固定，横横页面布局适应顺序正常
- 判断题和选择题属于不同题目环节
- iPad 端基于手机端截屏大小，不受拉伸宽度影响

#### 颜色

- 选项底色：`#E3E3E3`（默认）
- 选中正确：绿色系
- 选中错误：红色系
- 颜色不固定，根据学科色进行调整（对标 XY 轴上下显示）

### 3.4 拖拽填空

#### Phone 横屏 / iPad 横屏

四种填充状态：

| 状态 | 视觉表现 |
|------|----------|
| 拖拽有内容 | 橙底白字（实心填充，学科色） |
| 置空 | 米色/浅黄底，无文字 |
| 已填 | 橙色描边 + 橙色文字（学科色） |
| 未填 | 浅底 + 虚线边框 |

#### 适配与颜色

- 颜色不固定，根据学科色进行调整
- 边制基础规则：投影、圆角按原始大小不固定，照横填完整保持主题风格及色调
- iPad 以 Phone 为基本横比放大

### 3.5 翻页器

#### 显示格式

`XX/XX`（如 01/06、05/06、06/06）

左右箭头按钮 + 中间页码。

#### 按钮状态

| 状态 | 说明 |
|------|------|
| 首页 | 左箭头禁用（灰色），右箭头可用 |
| 中间页 | 左右箭头均可用（学科色高亮） |
| 末页 | 左箭头可用，右箭头禁用（灰色） |

#### 翻页 Button 状态

三种状态：**默认**（学科色）→ **点击**（深色） → **禁用**（灰色）

#### 适配说明

- 颜色不固定，根据学科色进行调整
- 翻页器属于导航的一部分，适配规则同导航一致

### 3.6 图标（学习环节 Icon）

#### 颜色语义

| 颜色 | 含义 | 示例 |
|------|------|------|
| 蓝色 | 常规类 | 写作主题、写作技法、我的行文 |
| 黄色 | 用户内容类 | 初始案例卡、写作技巧/互评方案/编辑改善、提示标识 |
| 橘色 | 反馈类 | 精选点评、写作点题、课文题跟、练写分析 |

#### 适配说明

- iPad 适配以竖屏为基本横比放大，iPad 横屏基于 Phone 端 1.5 倍放大
- 颜色会根据学科色变化
- 讲解入口 icon（大圆形）：颜色 + 圆图标，需根据学科和页面背景色调整

---

## 四、学科色体系

### 品牌色

`#6072FF`（紫蓝色，RGB: 124, 114, 252，HSB: 246, 55, 100）

### 学科色定义

| 学科 | 主色 | 色系 | RGB 参考 |
|------|------|------|----------|
| 语文 | `#FF5F33` | 暖橙红 | 255, 61, 61 / HSB: 13, 63, 90 |
| 数学 | `#1A02FF` | 紫色 | 0, 148, 253 / HSB: 265, 85, 100 |
| 英语 | `#FF8D00` | 橙色 | 255, 136, 0 / HSB: 35, 92, 100 |
| 物理 | `#3C6B5C` | 蓝绿 | 10, 54, 82 / HSB: 145, 55, 97 |

### 学科辅助色

| 类别 | 色系 | 说明 |
|------|------|------|
| 文科色 | 暖色（橙色系） | 用于语文等文科场景 |
| 理科色 | 冷色（待补充） | 用于物理等理科场景 |

### 基础色板

每个学科有完整的 **6 级渐变色板**（从浅到深），用于不同层级的背景、文字、强调等：

- 语文：橙色渐变（浅杏 → 深橙红）
- 数学：紫色渐变（浅紫 → 深紫）
- 英语：蓝绿渐变（浅青 → 深蓝绿）
- 物理：蓝色渐变（浅蓝 → 深蓝）

### 颜色分层模型（从底层到顶层）

| 层级 | 名称 | 说明 |
|------|------|------|
| 1 | 背景层 (Background) | 页面整体背景色，随学科色渐变 |
| 2 | 内容层-文科色 (Content) | 内容区域的填充色，区分 Card、文本区、高亮区等 |
| 3 | 内容层-学科色 (Content) | 学科专属的内容强调色 |
| 4 | 全局控制层 (Global Control) | 顶部导航栏色、按钮色、进度条色等 |

---

## 五、字重规范

### 学习环节字重定义

| 用途 | 字重 | 字号 |
|------|------|------|
| 标题字体 | **Bold** | 18pt |
| 精题字体 | **Bold** | 16pt |
| 正文字体 | Regular | 16pt |
| 题干字体 | Regular | 16pt |
| 选项字体 | Regular | 14pt |

### 规范应用示例

- 标题（如"一场足球赛"）：Bold 18pt，学科色或深色
- 精题文字（如"精彩描写"标签）：Bold 16pt，学科色高亮
- 正文（如题目内容、文章段落）：Regular 16pt，主要文字色
- 选项文案（如 A/B/C/D 选项）：Regular 14pt，次要文字色

---

## 六、布局示例速查

### Phone 横屏典型布局

- **基础框架**：左侧对话流/文章 + 右侧板书/互动区，两栏比例灵活
- **面板叠加**：内容面板覆盖在板书区上方
- **全屏**：特殊环节独占屏幕

### iPad 横屏典型布局

- **两栏大框**：左右各一个大内容区域
- **三栏通知栏**：导航通知 + 主内容 + 侧栏
- **特殊框架**：三栏 + 通栏混合

### Phone 竖屏典型布局

- **通栏框架**：如对话流、素材收集、灵感碰撞等场景
- **卡片框架**：如起笔成文、素材卡、成就结算等场景

### iPad 竖屏典型布局

- 基本复用横屏的两栏/三栏结构，按竖向重排
- 特殊框架保持独立适配
