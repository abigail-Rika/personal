# 项目协作索引

这是 Codex、wb 和文悦共同使用的项目入口。

## 使用规则

- 开始项目工作前，先读本文件，再读对应项目目录下的 `PROJECT_STATUS.md`。
- 只有需要持续推进、会产生多个工作产物或需要多个 AI 协作的事项，才登记为项目。
- 日常笔记、单次记录和归档内容不在这里登记。
- `归档/` 下内容是历史存档，不作为进行中的项目维护。
- 新项目先复制根目录的 `PROJECT_STATUS_TEMPLATE.md`，放到项目目录并命名为 `PROJECT_STATUS.md`，再在下表登记。

## 项目列表

| 项目 | 路径 | 状态文件 | 当前状态 | 最后更新 |
|---|---|---|---|---|
| 暂无已登记的进行中项目 | — | — | — | — |

已有产物但尚未确认是否作为项目维护的目录，不自动纳入上表；确认后再登记，避免把临时输出误当成长期项目。

## 状态定义

- `active`：正在推进
- `blocked`：有明确阻塞，等待外部信息或决策
- `paused`：暂时搁置，但之后可能继续
- `done`：目标已完成，保留状态文件供回溯
- `archived`：不再维护，仅作历史参考

## 协作约定

1. 开始工作：读取状态文件，确认当前阶段、进行中事项、阻塞和下一步。
2. 认领工作：在状态文件的“进行中”区域写明执行者（`Codex`、`wb` 或“文悦”）和目标，避免重复劳动。
3. 完成工作：更新完成事项、验证结果、关键决策、阻塞和下一步，并写入最后更新时间与更新者。
4. 发现状态文件在读取后被修改：重新读取并合并，不用旧内容覆盖新内容。
5. 不把未经验证的推测写成结论；不删除其他协作者留下的历史记录。

